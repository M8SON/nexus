# Codex Adapter

Thin pointer file for Codex-based agents. Drop `AGENTS.md` (or symlink it) into a target repo so Codex picks up shared forge policies without duplicating their content.

All shared rules live under `forge/forge/policies/` and `forge/forge/skills/`. Do not inline policy text here.
