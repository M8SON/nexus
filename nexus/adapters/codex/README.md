# Codex Adapter

Thin pointer file for Codex-based agents. Drop `AGENTS.md` (or symlink it) into a target repo so Codex picks up shared nexus policies without duplicating their content.

All shared rules live under `nexus/nexus/policies/` and `nexus/nexus/skills/`. Do not inline policy text here.
