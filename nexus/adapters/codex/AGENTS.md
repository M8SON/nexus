# Codex Adapter

Use nexus shared policies, continuity rules, and recall workflow for repos under the configured workspace (`$NEXUS_WORKSPACE_ROOT`, defaulting to the directory that contains the nexus repo).

The behavioral baseline is the Karpathy-derived core policy: think before coding, simplicity first, surgical changes, goal-driven execution. Read it in full — it overrides default Codex behavior where they conflict.

See:
- `../../policies/core.md` — Karpathy-derived behavioral baseline (core feature of nexus)
- `../../policies/continuity.md` — recall and local-doc usage rules
- `../../skills/README.md` — reusable skill prompts
