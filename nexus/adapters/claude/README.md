# Claude Adapter

Thin pointer file for Claude-based agents. Drop `CLAUDE.md` (or symlink it) into a target repo so Claude picks up shared forge policies without duplicating their content.

All shared rules live under `forge/forge/policies/` and `forge/forge/skills/`. Do not inline policy text here.
