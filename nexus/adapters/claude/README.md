# Claude Adapter

Thin pointer file for Claude-based agents. Drop `CLAUDE.md` (or symlink it) into a target repo so Claude picks up shared nexus policies without duplicating their content.

All shared rules live under `nexus/nexus/policies/` and `nexus/nexus/skills/`. Do not inline policy text here.
